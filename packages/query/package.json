{
  "name": "@vetster/orval-query",
  "version": "6.28.2",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git@github.com:vetster/orval.git"
  },
  "scripts": {
    "build": "tsup ./src/index.ts --target node12 --clean --sourcemap --dts",
    "dev": "tsup ./src/index.ts --target node12 --clean --sourcemap --watch src",
    "lint": "eslint src/**/*.ts",
    "test": "vitest",
    "bump-to": "yarn version --no-commit-hooks --no-git-tag-version --new-version"
  },
  "dependencies": {
    "@vetster/orval-core": "6.28.2",
    "lodash.omitby": "^4.6.0"
  },
  "devDependencies": {
    "@types/lodash.omitby": "^4.6.7",
    "vitest": "^0.34.6"
  }
}
